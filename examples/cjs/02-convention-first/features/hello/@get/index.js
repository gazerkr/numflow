const numflow = require('numflow')

// ⭐ Core: Empty object!
// method: 'GET' ← Auto-inferred from @get folder!
// path: '/hello' ← Auto-inferred from folder structure!
// steps: './steps' ← steps/ folder auto-detected!
module.exports = numflow.feature({
  // Nothing needed! Everything is automatic!
})
